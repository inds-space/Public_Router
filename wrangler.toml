name = "router"
main = "index.js"
compatibility_date = "2024-09-23"

# Custom domains — CF auto-issues SSL certs, no dummy DNS A records needed.
# Add a new [[custom_domains]] block for each source in redirects.txt.

[[custom_domains]]
pattern = "inds.space"

[[custom_domains]]
pattern = "old.inds.space"

[[custom_domains]]
pattern = "go.inds.space"
